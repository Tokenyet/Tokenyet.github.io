# Google site verification

Place Google's generated verification HTML file in this directory when Google Search Console or Google Auth Platform gives you one.

The site also supports the meta-tag flow through `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` at build time.
